// Фикстура живых проверок: поставщик и товары. ОДИН источник для посева (`seed-test-portal.mjs`) и
// для прогона (`live-crm-sync.mjs`).
//
// ⚠ Копий было три — документ в прогоне, список посева и артикул, зашитый в самой проверке подбора.
// Разъезд между ними не роняет ничего: переименовали артикул в посеве — проверка подбора уходит в
// ветку «товара в каталоге нет, проверять не на чем» и печатает предупреждение, неотличимое от
// штатного прогона на портале без посева. То есть утверждение исчезает МОЛЧА — ровно тем способом,
// против которого оно и написано.

/** Поставщик из документа. Опознаётся по налоговому номеру — тем же ключом, что и в приложении. */
export const SEED_SUPPLIER = { name: 'ООО «Тест-Поставщик»', taxId: '7712345678' }

/**
 * Товары каталога.
 *
 * ⚠ Название в каталоге ОБЯЗАНО отличаться от написания в документе (префикс `[TEST] `), и это не
 * косметика: доказательство подбора — то, что портал подставил в строку каталожное имя. Совпади
 * они, проверка стала бы тавтологией — то же имя пришло бы и через запасной подбор по имени при
 * полностью сломанной ветке артикула. Инвариант проверяется в самом прогоне, а не подразумевается.
 */
export const SEED_PRODUCTS = [
  { docName: 'Кабель ВВГ 3х2.5', article: 'KAB-325', price: 1.2 },
  { docName: 'Автомат С16', article: 'AVT-C16', price: 4.5 },
  { docName: 'Провод ПВС 2х1.5', article: 'PVS-215', price: 0.8654 }
]

/** Каталожное название по названию из документа. */
export const catalogNameFor = docName => `[TEST] ${docName}`

/** Внешний код товара в каталоге — по нему посев и находит свою строку. */
export const seedXmlId = article => `AIPI_SEED_${article}`
