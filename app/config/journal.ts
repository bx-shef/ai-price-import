// Размер страницы журнала — ОДИН на клиент и сервер (#495).
//
// ⚠ Два независимых числа разъехались бы молча: клиент считал бы смещение страницы по своему
// размеру, сервер отдавал бы по своему, и навигация перескакивала бы записи — причём тем заметнее,
// чем длиннее журнал. Серверная константа (`server/utils/importJournal.ts`) обязана совпадать, и
// это закреплено тестом.
export const JOURNAL_PAGE_SIZE = 50

/**
 * После какой по счёту записи журнала показывать врезку издателя (#507).
 *
 * ⚠ Не «сверху» и не «в конце». Сверху баннер загораживал работу — на телефоне до журнала нужно
 * было проскроллить пять экранов, и один занимало промо во весь экран. В конце его не увидит никто:
 * страница вмещает 50 записей. Третья строка — компромисс: человек уже увидел то, за чем пришёл, и
 * ещё не ушёл с экрана.
 */
export const PROMO_AFTER_ROW = 3
