// Имя продукта — ОДИН источник (#412).
//
// ⚠ Копий было три: `failureNotify.APP_NAME`, профиль бота в `chatBot.ts` и подпись тревог в
// `queueAlertDeliver.ts`. Критерий приёмки задачи — «имя в карточке Маркета, в шапке лицензии, в
// шапке Политики и в интерфейсе совпадает СИМВОЛ В СИМВОЛ»; три независимые строки это ровно тот
// случай, когда одна однажды поедет, а заметят по чужому скриншоту.
//
// ⚠ В самом ИМЕНИ нет слова «Битрикс24»/«Bitrix24» — это требование юридического пакета: чужой
// товарный знак в собственном названии продукта. Принадлежность к платформе живёт в описании и в
// подзаголовке, где она уместна и вопросов не вызывает (`LANDING_SUBTITLE`, тексты карточки).

/** Каноническое имя продукта. Пишется везде одинаково. */
export const APP_NAME = 'AI-импорт прайсов'

/**
 * Технический слаг: имя пакета, `User-Agent` к чужим сервисам, префиксы ключей хранилища.
 *
 * ⚠ Это НЕ имя приложения в Маркете (`shef.priceimport`, `app/utils/landing.ts`) и НЕ имена
 * образов/контейнеров/базы на сервере — те менять нельзя без действий на самой машине, иначе
 * развёртывание тихо застынет на прежнем образе.
 */
export const APP_SLUG = 'ai-price-import'
