import type { PendingLegalEdition } from '~/utils/legalNotice'

// Объявленные, но ещё не вступившие в силу редакции документов (#418). Данные, не логика.
//
// ⚠ Первая настоящая запись здесь появилась 08.08.2026 — уточнение п. 5.6 Политики приложения
// (#468): описание служебного канала оповещений было у́же того, как канал работает после #466.
// Получатель и состав передаваемых данных не изменились, поэтому изменение НЕ существенное
// (п. 13.2) — срок 10 дней.
//
// ⚠ Объявление ОДНО, на Политику. Лицензия в этот проход не менялась по существу (у неё поменялась
// только напечатанная дата вступления), и объявлять «изменения» там, где их нет, значит приучить
// лицензиата не читать уведомления. Документы САЙТА лицензиата не связывают — уведомлять о них
// некого, так сказано в шаге 8 руководства.
//
// ⚠ Порядок публикации редакции — ОДИН, и он в `docs/PROCESS.md` §6.11 (шаги 8–12 про это
// объявление). Своей версии шагов здесь нет намеренно: два порядка разъехались бы, а половина
// работы, сделанная по одному из них, не даёт ни одного симптома — уведомления просто не будет.
//
// ⚠ Текст самого документа меняется в тот же проход, что и запись здесь: баннер, обещающий новую
// редакцию, ссылается на её постоянный адрес в архиве, и без снимка ссылка ведёт в 404.
export const PENDING_LEGAL_EDITIONS: PendingLegalEdition[] = [
  {
    slug: 'privacy',
    title: 'Политика конфиденциальности приложения',
    date: '2026-08-08',
    publishedAt: '2026-08-08',
    material: false,
    changes: [
      'Уточнено описание служебного канала оповещений Издателя (п. 5.6): он охватывает не только сообщения о сбоях, но и внутренние сводки о работе Приложения — например, о количестве отзывов, ожидающих разбора.',
      'Состав передаваемых данных не расширяется: как и прежде, передаются только имена внутренних очередей и процессов Издателя и числовые показатели. Данные Клиента в такие сообщения не попадают.',
      'Перечень получателей не изменился.'
    ],
    // Прежней редакции нет: эта — первая.
    previousDate: null
  }
]
