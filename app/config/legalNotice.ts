import type { PendingLegalEdition } from '~/utils/legalNotice'

// Объявленные, но ещё не вступившие в силу редакции документов (#418). Данные, не логика.
//
// ⚠ Список ПУСТОЙ, и это рабочее состояние, а не заготовка. Механизм обязан существовать ДО первого
// изменения документов: писать уведомление в спешке, когда правка уже понадобилась, — верный способ
// сделать его юридически негодным. Пока список пуст, баннера нет нигде и в чат ничего не уходит.
//
// Как публиковать редакцию (один проход, порядок важен):
//   1. положить снимок в `docs/archive/<документ>/<дата>.md` и внести его в `LEGAL_ARCHIVE`
//      (у прежней редакции проставить `supersededAt`);
//   2. добавить сюда запись: `publishedAt` — сегодняшний день, `material` — существенное ли
//      изменение, `changes` — что меняется ПО СУЩЕСТВУ (две-три строки, не «условия обновлены»);
//   3. дату вступления НЕ вбивать: её считает `effectiveDate` из срока (10 или 30 дней) —
//      рукописная рано или поздно окажется короче обещанного;
//   4. после даты вступления запись отсюда убрать — баннер и так исчезнет сам, но лишняя запись
//      будет вводить в заблуждение следующего читателя.
//
// ⚠ Текст самого документа меняется в тот же проход, что и запись здесь: баннер, обещающий новую
// редакцию, ссылается на её постоянный адрес в архиве, и без снимка ссылка ведёт в 404.
export const PENDING_LEGAL_EDITIONS: PendingLegalEdition[] = []
