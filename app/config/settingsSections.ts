/**
 * Разделы экрана настроек: якорь, заголовок и подпись.
 *
 * ⚠ Живут в конфиге, а не в одном из двух файлов, которые их читают: якоря нужны И форме
 * (`SettingsForm.vue` ставит их на секции), И странице (`pages/settings.vue` строит по ним полосу
 * разделов в тулбаре). Разъехавшись, они дают ссылку в никуда — переход просто ничего не делает,
 * и снаружи это неотличимо от «страница подвисла».
 *
 * ⚠ Схлопывание разделов НЕ вернулось и не вернётся без разбора (#259): в аккордеоне закрытая
 * секция ПРЯТАЛА настройки, и «что вообще можно настроить» было видно только по заголовкам. Теперь
 * все блоки развёрнуты и стоят друг под другом, а тулбар ведёт по якорям.
 */
export const SETTINGS_SECTIONS = [
  { id: 'routing', label: 'Куда импортировать', description: 'Целевая сущность по умолчанию и правила: какие документы куда вносить.' },
  { id: 'products', label: 'Товары и единицы', description: 'По какому полю искать товар в каталоге, что делать с ненайденными и как читать единицы измерения.' },
  { id: 'notify', label: 'Уведомления', description: 'Чаты, куда приходят сообщения об импорте и об ошибках.' },
  { id: 'savings', label: 'Экономия', description: 'Ставка часа для плитки «Сэкономлено денег» на главном экране.' }
] as const

export type SettingsSectionId = typeof SETTINGS_SECTIONS[number]['id']
