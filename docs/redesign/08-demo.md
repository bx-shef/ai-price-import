# Демо на лендинге — карта продукта и журнал работ

> Last reviewed: 2026-07-14

Публичное демо на лендинге: пользователь прикрепляет файл (КП / счёт / ТТН) → получает ответ
«что за подрядчик, какие товары и т.п.». Плюс готовые примеры (рус/бел/каз) и возможность
загрузить свой файл. **Дизайн/тексты/UX правим потом — сейчас главное, чтобы всё отрабатывало.**

Этот документ — источник правды по демо. **Перечитывать перед каждым новым куском работы** и
держать журнал внизу синхронно.

## Требования (из задачи владельца)

1. Прикрепить файл → ответ: подрядчик (поставщик), товары, тип документа, суммы.
2. Дать 2–3 примера (или сгенерировать самому): КП, счёт, ТТН на рус/бел/каз.
3. Пользователь может загрузить свой файл — **предупредить о публичности** и **ограничить
   3 файла за 10 минут**.
4. Баннер-текст: «если нужно доработать — сделаем на ваших серверах».
5. Всё должно реально отрабатывать (UX/дизайн — потом).

## Окружение (проверено вживую)

- **Прод-образ несёт AI-конвейер**: бинарники извлечения (`pdftotext`/`libreoffice`/`tesseract`
  rus+bel+kaz+eng) + агент (Claude Code CLI против DeepSeek через Anthropic-совместимый шлюз).
  В dev-контейнере/CI их нет — поэтому AI-путь **проверяется в проде** (как основной конвейер);
  чистое ядро маппинга покрыто юнит-тестами с моками.
- Bitrix24 в демо **не** участвует (ни токенов, ни записи) — только разбор и показ.

## Архитектурное решение (P5-b, #45)

Демо **маршрутизирует по формату** — детерминированно там, где можно, AI там, где нужно:

- **Текст / CSV / XLSX → детерминированный экстрактор** `app/utils/demoExtract.ts` (мгновенно,
  бесплатно, покрыт тестами): тип документа (КП/счёт/ТТН по ключевым словам рус/бел/каз),
  поставщик (метка + налоговый ID УНП/ИНН/БИН/БСН), таблица товаров, итоги, **валюта** (₽ RUB, ₸ KZT, знак НБ РБ для BYN — см. «Значки валют»),
  НДС и «Всего». `.xlsx` читается через `xlsxToText` (exceljs). **exceljs падает** на части
  реальных книг (рисунок в колонтитуле → `reading 'anchors'`), поэтому при любой ошибке загрузки —
  **фолбэк** `xlsxToTextFallback` (свой мини-ридер на `node:zlib`, читает sharedStrings + первый лист,
  без exceljs; XML сканируется `indexOf`, не бэктрекающими regex → O(n); каждый инфлейт ограничен
  `maxOutputLength`; капы строк/колонок/длины ячейки — безопасен на публичном эндпоинте). GH #65.
  Ограничения фолбэка (в отличие от exceljs, документированы): даты отдаются сырым excel-serial
  (нет `styles.xml`); «первый лист» выбирается по номеру файла `sheetN.xml`, а не по порядку вкладок
  из `workbook.xml`. Для демо-извлечения приемлемо.
- **PDF / скан (.png/.jpg) / Word / `.xls` → реальный AI-конвейер** `server/utils/demoAi.ts`
  (`runDemoAiExtract`): temp-файл → `extractText` (poppler/libreoffice/OCR) → `runAgent` (DeepSeek) →
  чистый маппер `extractedToDemoResult` (те же поля, включая валюту и НДС) → структура **или
  честная ошибка** (никогда 500 на плохой документ). Temp-файл удаляется в `finally`. `.xls` (старый
  бинарный Excel, выгрузки 1С) exceljs не читает → идёт через libreoffice (`officeToText`), как `.doc`.
  Для табличных форматов `officeToText` экспортирует в **CSV с закреплёнными опциями** (TAB-разделитель
  `9`, UTF-8 `76` — не зависят от локали контейнера, TAB не путается с десятичной запятой). GH #64.
  > **Сканированный PDF → OCR через растеризацию (GH #100).** Цифровой PDF читается `pdftotext`; если
  > текста почти нет (скан) — фолбэк на OCR. tesseract НЕ читает PDF напрямую, поэтому `ocr` определяет
  > `%PDF-` и растеризует страницы `pdftoppm` → PNG → OCR каждой **последовательно** (бережём CPU/RAM),
  > в числовом порядке страниц; кап страниц (`-l`) и размера (`-scale-to`) — в самом pdftoppm. Картинки
  > (png/jpg) идут прямым OCR как раньше. Смешанные (цифра+скан) PDF: решение по суммарному тексту, скан-
  > страницы могут не получить OCR (см. `MIN_PDF_TEXT`).
  > **Все листы, не только первый (GH #76).** Фильтр несёт токен «экспорт листов» `-1` → libreoffice
  > пишет по CSV на лист, `officeToText` склеивает их **в порядке книги** (парсит stdout, не сортирует
  > имена — иначе шапка-лист уехал бы в хвост). Иначе товары на не-первом листе (у ТТН — «Приложение»)
  > терялись → 0 позиций. Пустые листы отбрасываются; число листов ограничено `MAX_SHEET_CSVS`. **NB:
  > это про боевой `officeToText` (libreoffice). Демо-фолбэк `xlsxToTextFallback` (см. выше) по-прежнему
  > берёт только первый лист** — рассинхрон демо↔прод, follow-up #76.
  > Фильтр выбирается по **имени файла** (`officeToText(path, fileName)`), а не по пути на диске —
  > поэтому CSV-ветка работает и в демо (temp `<uuid>.xls`), и в in-portal пайплайне, где файл лежит
  > как `<jobId>.bin`, но исходное имя доходит как `fileName` (GH #74). libreoffice определяет формат
  > входа по содержимому, так что `.bin`-путь корректен.
  > ⚠️ Качество `.xls` через libreoffice в dev-контейнере **не воспроизвести** (libreoffice здесь не
  > открывает даже нормальный `.xlsx`) — проверять на **проде** после деплоя.
- **Backend** `POST /api/demo/extract`: rate-limit 3 файла / 10 мин на IP + **глобальный лимит
  одновременных AI-разборов** (`DEMO_AI_MAX_CONCURRENCY`, деф. 2 → 503 при перегрузке); потолок
  **5 МБ**; флаг публичности. nginx `client_max_body_size 6m` на этом роуте.
  **Детерминированный путь** (txt/xlsx) — синхронный, ответ сразу `{result}`. **AI-путь — асинхронный**
  (GH #70): submit сразу возвращает `202 {jobId}`, тяжёлый OCR идёт **в фоне**, клиент опрашивает
  `GET /api/demo/result/:jobId` → `{status:'pending'}` / `{status:'done', result}` / `422 {status:'error'}`
  / `404` (задача устарела). Так HTTP-запрос короткий и **не упирается в шлюз-таймаут** (было 504 на
  тяжёлых сканах). Job-store — in-memory (`server/utils/demoJobStore.ts`, чистый + тесты), с TTL 5 мин
  и жёстким капом 128 задач (свип + `null`→503 при переполнении); поллинг — дешёвый O(1) lookup по
  неугадываемому UUID, квоту 3/10мин не тратит. TTL job-а (15 мин) заведомо **больше** окна поллинга
  (5 мин), чтобы результат, посчитанный под конец, не потерялся.
  > ⚠️ Store **in-memory, только один инстанс**: задачи не переживают рестарт бэкенда (при деплое во
  > время активного поллинга клиент получит 404 → перезалить файл) и не общие между репликами. Прод-демо
  > = один backend-контейнер. При масштабировании/репликации — перенести store на Redis (`server/queue`).
- **UI** `app/components/DemoTryout.vue` (секция `#demo`, визуально выделена): дропзона + кнопки
  примеров + **прогресс-бар** разбора + карточка результата (валюта в шапках/итогах).
- **Готовые примеры** — `public/demo/*` (**18 файлов**: КП/счёт/ТТН × ru/be/kk в `.txt` **и** `.pdf`).
  Кнопки-примеры парсят `.txt` **на клиенте** (мгновенно, без лимита); ссылка «скачать PDF» отдаёт
  реалистичный PDF, который можно загрузить обратно (путь AI-конвейера).

## Формат демо-файлов

- **Текстовые** (`.txt`, UTF-8) — детерминированно-парсимые: заголовок с типом и номером, блок
  «Поставщик … УНП/ИНН/БИН …», таблица товаров (Наименование | Кол-во | Ед. | Цена | Сумма) + итоги.
  Реквизиты по странам: **РФ** ИНН+₽+НДС 22%, **РБ** УНП+BYN (знак НБ РБ)+НДС 20%, **КЗ** БСН+₸+ҚҚС 12%.
- **PDF** — генерируются из `.txt` (`pnpm demo:pdfs`, Chromium), коммитим как статику; скачиваемы
  с лендинга и проходят через реальный AI-конвейер при загрузке.
- **Принимаемые загрузки**: `.txt/.csv/.tsv`, `.xlsx`, `.xls`, `.pdf/.png/.jpg/.jpeg/.docx/.doc` до 5 МБ.
  (`.xlsx` — детерминированно; `.xls` — через AI-путь/libreoffice, т.к. exceljs его не читает.)

## Значки валют

Валюта в карточке результата (шапки колонок и итоги) рисуется компонентом
`app/components/CurrencySign.vue` по ISO-4217 коду (`:code`). Правило простое:

- **Есть Unicode-знак** (RUB → ₽, KZT → ₸, USD → $, EUR → €, …) — рендерим его как **текст**
  через `currencySymbol(code)` из `app/utils/demoExtract.ts`. Копипаст и скринридер работают сами.
- **Нет Unicode-кодпоинта** — единственный такой случай сейчас **BYN**. У белорусского рубля нет
  символа в Unicode, поэтому встраиваем **официальный знак НБ РБ** (трассированный контур,
  `fill="currentColor"` — наследует цвет и кегль текста). Рядом — визуально скрытый, но
  **выделяемый** `<span>BYN</span>`: скринридер озвучивает, копипаст даёт реальный текст, а не пусто.
  Источник глифа — [Wikimedia Commons: BYN_symbol.svg](https://commons.wikimedia.org/wiki/File:BYN_symbol.svg),
  помечен **Public domain** (шаблоны `PD-author`/`PD-Layout`/`PD-icon`); плюс это официальный
  государственный денежный знак — использование свободно, атрибуции не требует.

Как добавить новую валюту:

1. Добавь пару `код → символ` в `currencySymbol` (`app/utils/demoExtract.ts`) — если у валюты есть
   Unicode-знак, этого достаточно, отдельная ветка в `CurrencySign` не нужна.
2. Если Unicode-знака **нет** (как у BYN) — добавь `v-else-if="code === '<КОД>'"` ветку в
   `CurrencySign.vue` со встроенным SVG (заливка `currentColor`, `viewBox` по исходнику,
   `aria-hidden`) **плюс** скрытый выделяемый текст-код для a11y/копипаста. Источник глифа
   (файл + лицензию) укажи комментарием прямо в SVG (как у BYN — ссылка на Wikimedia Commons).
3. Заведи для новой валюты **свой** CSS-класс (по образцу `.byn-sign`, но не переиспользуя его):
   `height`≈`0.9em`, `vertical-align` под шрифт — чтобы глиф сидел на базовой линии наравне с
   текстовыми знаками. У каждого глифа своя метрика, общий класс переиспользовать не стоит.

Не использовать `v-html` для знака и не хардкодить символы в шаблонах карточки — только через
`CurrencySign`, чтобы правило рендера жило в одном месте.

## Безопасность демо

- Публичность: предупреждение в UI + в ответе; файлы не сохраняем дольше обработки
  (temp-файл AI-пути удаляется в `finally`).
- Rate-limit 3/10мин на IP (in-memory окно; за прокси клиент — **последний** хоп
  `X-Forwarded-For`, который nginx дописывает и клиент не может подделать) **плюс** глобальный
  лимит одновременных AI-разборов (`DEMO_AI_MAX_CONCURRENCY`, деф. 2 → 503 при перегрузке).
- Потолок 5 МБ (nginx `client_max_body_size 6m` на роуте); `Content-Length` обязателен (411);
  допустимые форматы по расширению; никакого исполнения загруженного.
- Никаких секретов/Б24 в демо-пути.

## Журнал работ (свежее сверху)

- **2026-07-14 (5)** — **GH #74** office-таблицы (`xls/xlsx/ods`) в **in-portal** пайплайне
  извлекались `txt`-фильтром, а не CSV: `officeToText` выбирал фильтр по пути на диске, а там файл
  лежит как `<jobId>.bin` (расширение потеряно). Исправлено: `officeToText(path, fileName)` — фильтр
  по **имени файла** (доходит как `fileId`=оригинальное имя), а `path` (`.bin`) libreoffice читает по
  содержимому. Теперь CSV работает и в демо, и в проде. Тест: office-ветка получает оба аргумента.
  Итог: 527 тестов, lint, typecheck — зелёные.
- **2026-07-14 (4)** — **GH #70** AI-путь демо переведён на **асинхронную** обработку вместо
  синхронного ожидания в HTTP (симптом 504 на тяжёлых сканах лечился ростом таймаутов в #69 — теперь
  устранён по существу). `POST /api/demo/extract`: AI-файлы регистрируют job (`server/utils/demoJobStore.ts`,
  in-memory, TTL 5 мин, кап 128, чистый + DI на `now`/`genId`), обрабатываются **в фоне** (сохранён
  concurrency-guard `DEMO_AI_MAX_CONCURRENCY`), submit возвращает `202 {jobId}`. Новый роут
  `GET /api/demo/result/[jobId]` — поллинг (pending/done/error/404, без квоты). `DemoTryout.vue` поллит
  каждые 2с до 5 мин; детерминированный путь (txt/xlsx) остался синхронным (backward-compat: ответ
  либо `{result}`, либо `{jobId}`). Плюс поднят `hookTimeout` nuxt-проекта (флейк холодного старта).
  **По 5 проверяющим:** TTL job-а поднят до 15 мин (> окна поллинга 5 мин), иначе результат под конец
  терялся молча; клиентский `pollJob` вынесен в чистую `app/utils/demoPoll.ts` (DI на fetch/sleep/clock)
  + тесты; дедуп 503-блоков; явно задокументировано ограничение single-instance (in-memory store не
  переживает рестарт/реплики → при масштабировании перенести на Redis, ISSUE). Тесты: job-store (11),
  poll (4). Итог: 527 тестов, lint, typecheck — зелёные. Прим.: AI-путь (LLM/бинарники) в dev не
  воспроизвести — сквозной async-поток проверять на проде.
- **2026-07-14 (3)** — **GH #66** качество детерминированного парсера (по анализу 42 файлов):
  (1) объединённые ячейки — exceljs дублировал значение по всем колонкам слияния (шапка/поставщик
  ×N ломали детект) → в `xlsxToText` теперь значение только master-ячейки, слейвы пустые (фолбэк и так
  не дублирует); (2) заголовок кол-ва `Коли-\nчество` (мягкий перенос) не матчился → нормализуем `-\s+`
  в `mapHeader` (кол-во распозналось для реальных таблиц); (3) подвальные строки (`Ответственный`,
  `Изготовитель`, подписи) без чисел больше не считаются позициями (`NOISE_ROW`); (4) поставщик без
  метки «Поставщик:» — берём компанию сверху (`ООО/ОАО/ИП …`, `COMPANY_AT_TOP`) → поставщик извлечён
  для всех 10 xlsx датасета (было 3 без имени). Тесты: +5 (#66). Итог: 507, lint, typecheck — зелёные.
  Известное ограничение (не чиним — риск регресса): документы с **двумя** таблицами (счёт+спецификация
  со сдвинутыми колонками) — вторая таблица разбирается по колонкам первой; редкий кейс.
- **2026-07-14 (2)** — По анализу импорта 42 реальных документов: **GH #65** — `.xlsx` с рисунком
  в колонтитуле роняли exceljs (`reading 'anchors'`) → 422 (6 из 10 книг). Добавлен фолбэк
  `xlsxToTextFallback` (мини-ридер на `node:zlib`: sharedStrings + первый лист → TAB-текст);
  `xlsxToText` ловит ошибку exceljs и переключается на него. Проверено на всех 10 книгах датасета
  (6 крашивших теперь читаются). **GH #64** — `.xls` (бинарный Excel, 1С) демо отклоняло по
  расширению; добавлен в `DEMO_AI_EXT` → идёт через libreoffice (как `.doc`); `officeToText` для таблиц
  экспортирует в CSV с закреплёнными опциями (TAB/UTF-8).
  **По 5 проверяющим (безопасность/QA/программист/тех-дир/доки):** переписал фолбэк-парсер с
  бэктрекающих regex (ReDoS: O(n²), ~6с на 700КБ) на `indexOf`-скан O(n) (~19мс на 5МБ); добавил
  `maxOutputLength` на инфлейт (zip-бомба через подделку размера в central directory больше не
  раздувается); единый чтец central directory (убрал дублирование); multi-run `inlineStr`, CDATA,
  числовой выбор листа; вынес чистую `officeConvertTarget` под тест; закрепил CSV FilterOptions
  (риск порчи цен из-за локали). Тесты: +security-регрессы (ReDoS-линейность, отбой бомбы, кап
  ячейки), реальный catch-путь через `vi.mock('exceljs')`, hand-built ZIP на формы, что exceljs сам
  не пишет. Итог: 502 теста, lint, typecheck — зелёные. Ограничения (документированы выше): office-путь
  libreoffice в dev не воспроизвести → **`.xls` проверять на проде после деплоя**; CSV-ветка живёт
  только в демо-пути (in-portal теряет расширение → txt) — ISSUE. _(Снято в GH #74 — см. запись (5):
  фильтр выбирается по `fileName`, CSV работает и в in-portal.)_
- **2026-07-14** — Знак BYN в `CurrencySign.vue` переведён с ручной обводки на **официальный
  глиф НБ РБ** (трассированный контур из Wikimedia, `fill="currentColor"`); скрытый выделяемый
  текст `Br`→`BYN` (a11y/копипаст). Добавлен раздел «Значки валют» с правилом рендера и
  инструкцией как заводить новую валюту. Проверено в браузере (крупный/мелкий кегль), lint/typecheck зелёные.
- **2026-07-09 (4)** — Прогон 5 проверяющих, устранены замечания: **безопасность** — XFF брал
  ПЕРВЫЙ хоп, а nginx `$proxy_add_x_forwarded_for` дописывает реального клиента в КОНЕЦ → брал
  спуфнутый → лимит обходился; теперь берём ПОСЛЕДНИЙ хоп; требуем Content-Length (411) чтобы
  chunked-тело не буферилось без границ; sweep+cap лимитера от роста памяти. **Продукт** —
  отклонённый файл больше НЕ тратит квоту (валидация до `check`). **Корректность** — расширен
  словарь колонок (Продукция/Позиция/Услуга/Тауар…), знак числа для `(330,00)`/трейлинг-минус,
  строка без имени сохраняется под плейсхолдером + предупреждение. **Тесты** — вынес чистые
  хелперы `server/utils/demoUpload.ts` (ext/validate/decode) + тесты; добавил кейсы БИН/ИИН/ЖСН,
  unknown-язык, итоги invoice ru/be/kk, `;`-разделитель. **Доки** — README/CLAUDE дополнены 08.
  Итог: 369 тестов, lint, typecheck — зелёные. Известные ограничения (документированы): реальный
  PDF/Excel демо не парсит (нужен агент/бинарями — вне демо); имя с разделителем `|` внутри —
  позиционный сдвиг колонок.
- **2026-07-09 (3)** — Backend `POST /api/demo/extract` (`server/api/demo/extract.post.ts`):
  rate-limit 3/10мин на IP (чистый `server/utils/demoRateLimit.ts` + тесты), лимит 1 МБ,
  только текст (.txt/.csv/.tsv), декод UTF-8→CP1251, предупреждение о публичности; файл не
  сохраняется. UI `app/components/DemoTryout.vue` + секция на лендинге (`index.vue`, якорь
  `#demo`) + баннер «доработаем на ваших серверах». Примеры парсятся **клиентом** (мгновенно,
  без лимита) тем же ядром; загрузки пользователя — через backend (лимит+публичность+путь под
  будущий агент). Пойман и починен typecheck: h3 `setHeader` типовая неоднозначность → пишем
  заголовок через `event.node.res`. Итог: 355 тестов, lint, typecheck (app+server) — зелёные.
  PR #18. Дальше: 5 проверяющих → мерж.
- **2026-07-09 (2)** — Чистое ядро `app/utils/demoExtract.ts` + 9 примеров
  (`public/demo/{kp,invoice,ttn}-{ru,be,kk}.txt`) + тесты (`tests/demoExtract.test.ts`,
  16 шт, зелёные). Пойманы и исправлены баги: JS `\b` не работает с кириллицей/казахским
  (тип документа, налоговый ID, строки итогов уходили в позиции) → перешли на
  `(?<![\p{L}])…(?![\p{L}])` c флагом `u`; `parseNum('—')` возвращал 0 → требуем цифру.
  Ветка `claude/landing-demo-tryout` от свежего `main` (PR #17 смержен владельцем).
  Дальше: backend endpoint `POST /api/demo/extract` + rate-limit 3/10мин → UI-секция + баннер
  → 5 проверяющих → мерж.
- **2026-07-09** — Заведён документ карты продукта. Разведка окружения (нет LLM-ключа,
  нет pdftotext/tesseract в dev). Принято решение: детерминированное демо-ядро.
