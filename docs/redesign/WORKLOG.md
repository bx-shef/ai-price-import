# Рабочая карта: доведение до рабочего варианта (автономный прогон)

> Last reviewed: 2026-07-14

Живой трекер автономной работы по замечаниям владельца (он отошёл на 2 суток, решения
и мержи — на мне). **Приоритет: «должно всё отработать» (функциональность) > дизайн/тексты/UX.**
Процесс: перечитать эту карту → взять следующий незакрытый пункт → реализовать → 5 проверяющих
(Workflow) → починить → lint/typecheck/test → PR → мерж при зелёном CI → обновить статусы → отчёт %.
Гоняю на fresh-ветках от `main`; Watchtower на сервере сам подтянет новые образы.

## Прогресс: ~88%

Фундамент (деплой, установка в портал, фрейм-авторизация, бинарь агента) — готов (#26–#30).
Дальше — замечания ниже.

## Легенда статусов
`TODO` не начато · `WIP` в работе · `REVIEW` на проверяющих · `DONE` смёржено · `BLOCKED` нужен живой портал/владелец

---

## Приоритет 1 — Функциональность («должно всё отработать»)

| # | Пункт | Статус | PR | Примечания |
|---|-------|--------|----|-----------|
| P1 | E2E-конвейер: `etalon-invoice.pdf` → текст → агент DeepSeek → результат `done` | BLOCKED | — | Бинарь агента в образе (#30). Полная E2E-проверка in-portal требует iframe/живого ключа DeepSeek; проверяю через тест-портал (вебхук) + юнит/интеграционные тесты + детерминированные куски. |
| P2 | Метрики in-portal: просмотр + **сброс** (в рамках 1 Б24), возврат к метрикам после старта загрузки, показ «сэкономлено времени/денег» | DONE | #31,#32,#33 | Навигация ✅ (#31) + backend read/reset+savings ✅ (#32) + проводка счётчиков ✅ (#33) + UI-панель «Экономия» с временем/деньгами и сбросом на `/app` ✅ (P2-c). Значения оценочны (`SAVINGS_MODEL`, BYN) — тюнинг потом. |
| P3 | Доступ к странице **настроек** из `/app` (кнопка/ссылка) | DONE | #31 | Шестерёнка в шапке `/app` → `/settings`. 5 проверяющих — 0 замечаний. |
| P4 | **Мобильный режим** — визуальная проверка (скриншоты) + фиксы | DONE | этот PR | Заведён харнесс `pnpm screenshot` (`scripts/screenshot.mjs` + `playwright-core` на предустановленном Chromium) — снимает все роуты × mobile/desktop × light/dark в `screenshots/` (gitignored). Прогнал, посмотрел пиксели: `/app`,`/import`,`/settings`,`/`,`/queues`(auth-gate),`/login` в мобайле — чисто, без обрезки/переполнения, приложение работает. Doc — `docs/redesign/VISUAL_VERIFICATION.md`. Находка (отложено, дизайн): in-portal-страницы standalone — light-only (нет `app.config.ts` colorMode; тёмная тема лендинга — через класс `.landing-shell`). |
| P5 | **Демо** принимает любой документ (PDF/скан/Excel/Word/текст) | DONE | #37, #45 | **P5-a Excel** (#37). **P5-b — реализовано** (этот PR): демо-эндпоинт роутит по формату — text/csv/xlsx → детерминированно (мгновенно, бесплатно); **PDF/скан(.png/.jpg)/Word → реальный AI-конвейер**: temp-файл → `extractText` (poppler/libreoffice/OCR) → `runAgent` (DeepSeek) → структурированный результат (товары/контрагент/тип) **или честная ошибка**. Лимит 3/10мин, без хранения (temp с очисткой в `finally`), размер до 5 МБ. Чистое ядро (`server/utils/demoAi.ts`: маппер `extractedToDemoResult` + DI-оркестратор `runDemoAiExtract`) под тестами; живой путь (бинари+агент+ключи DeepSeek) — в прод-образе, проверяется в проде (как P1). UI: `DemoTryout` accept+подсказка. |
| P6 | **Нативные компоненты b24ui** вместо raw HTML (кнопки/инпуты/селекты/свитчи) на всех страницах | DONE | #39 | Замечание владельца: UI использует `<button>`/`<input>`/`<select>`/`role=switch` вместо b24ui. Переведены все in-portal-страницы: `settings.vue` (B24Button-сегменты/B24InputNumber/B24Input/B24RadioGroup/B24Select/B24Switch/B24Alert), `app.vue` (B24Button+иконки/B24Alert), `import.vue` (**B24FileUpload** — нативная дропзона), `login.vue` (B24FormField+B24Input+B24Button block), `queues.vue`. Сверено с офиц. llms.txt: убран несуществующий `variant="soft"` у B24Alert везде (+install.vue). 5 проверяющих — 1 замечание (лишний `id` на B24Input в login ломал бы label↔input связку B24FormField) устранено, остальное чисто. Проверено скриншотами. Лендинг `DemoTryout.vue` намеренно оставлен на брендовой тёмной вёрстке. |
| P7 | **Поле «Артикул поставщика»** — поисковый пикер (B24SelectMenu) вместо free-text | DONE | #40 | Замечание владельца (ссылка на select-menu.md + образец в client-bank). Порт async-подбора 1:1 из client-bank: `remoteSearch.ts`+тесты, `useRemoteSearch.ts`, `AsyncSearchSelect.vue` (над B24SelectMenu, `ignore-filter`+`v-model:search-term`+«Показать ещё»). Backend: `server/utils/catalogPropertySearch.ts` (чистое ядро+DI+тесты) — `catalog.catalog.list` (главный iblock = `productIblockId==null`) → `catalog.productProperty.list` → `{value:code,label:name}` + фильтр; роут `server/api/catalog-properties.get.ts` (фрейм-токен). `useCatalogProperties.ts` — фетчер. `settings.vue`: поле артикула → `AsyncSearchSelect` (min-chars=0). **Сверено вживую** (`B24_HOOK`, скоуп `catalog` есть в `B24_REQUIRED_SCOPES`): iblock 25 → `SUPPLIER_ARTICLE`/«Артикул поставщика». Скриншот — пикер рендерится нативно. 443 теста. |

## Приоритет 2 — Демо-контент

| # | Пункт | Статус | PR | Примечания |
|---|-------|--------|----|-----------|
| D2 | **Скачиваемые примеры на лендинге — PDF** (а не `.txt`) | DONE | этот PR | Замечание владельца: «скачать примеры — это текстовые, а нужны PDF». Клиент скачивает реалистичный PDF (счёт/КП/ТТН, РФ/РБ/КЗ) и загружает в демо — как раз путь AI-конвейера. `scripts/make-demo-pdfs.mjs` (+ `pnpm demo:pdfs`) рендерит `public/demo/*.txt` → стилизованный A4-PDF предустановленным Chromium (pipe-строки → таблица, итоги жирным); PDF коммитим как статику. Быстрые кнопки-примеры по-прежнему парсят `.txt` на клиенте мгновенно; ссылка «скачать» отдаёт PDF. Проверено рендером PDF в Chromium (вид счёта корректный). |
| D1 | Демо-доки РБ (`public/demo/*-be.txt`): «рахунак»→рус, **неверный номер**, русские названия для доков РБ, сделать **скачиваемыми**, устранить нестыковки | DONE | #35 | 3 дока РБ переведены на русский (контекст РБ: УНП/BYN/BY-счета/Минск), номер `РФ-7743`→`№ 743`, поставщики распутаны (было одинаковое имя с разными УНП), лейбл «Рахунак»→«Счёт», интро по странам (РФ/РБ/КЗ), **скачивание примеров** (иконка ⬇ у каждого) в `DemoTryout.vue`. Тесты обновлены (lang `ru`). KZ-доки — казахские (рынок). |

## Приоритет 3 — Лендинг-дизайн («потом поправим», но перечислено; порт из `client-bank-alfa-by`)

| # | Пункт | Статус | PR | Примечания |
|---|-------|--------|----|-----------|
| L1 | Favicon | DONE | #36 | `public/favicon.svg` (документ+стрелка импорта, cyan-акцент) + `nuxt.config` `app.head.link` — глобально на все страницы (лендинг + in-portal). SVG (совр. браузеры). |
| L2 | Шапка с визиткой | DONE | этот PR | Владелец: «это моя визитка» → порт 1:1 из client-bank (тот же ИП Шевчик): `BusinessCardModal` (фото `igor.jpg`, QR на `price-import.bx-shef.by`, vCard, контакты, реквизиты, booking) + `vcard`/`booking`/`clipboard`/`useMetrikaGoal`, dep `qrcode`. Адаптировано под продукт: `site` + роль «AI-импорт документов и товаров». Открывается из шапки (кнопка «Визитка») и hero-фото. Проверено браузером (фото грузится, модалка открывается, 0 ошибок). |
| L3 | Меню/навигация лендинга | DONE | #42 + этот PR | `LandingHeader` — липкая шапка. **Обогащено до паритета с client-bank** (замечание владельца «левое меню — у клиент банка верно»): полное меню Демо/Как это работает/Почему мы/**В Маркете**/**Реквизиты**/**Операторам** + «Визитка» + CTA на десктопе; **мобильный drawer** (гамбургер) со всеми пунктами. Проверено браузером (drawer открывается, нет h-скролла). |
| L4 | Фото в hero | DONE | этот PR | Фото владельца (`igor.jpg`) — кликабельный чип в hero под CTA («Игорь Шевчик · открыть визитку») → открывает `BusinessCardModal`. На мобайле это основной вход в визитку (nav шапки скрыт). |
| L5 | Ссылка на маркет Bitrix24 | DONE | этот PR | Ссылка **«В Маркете»** (`shef.priceimport`) в меню лендинга (десктоп-nav + мобильный drawer). Отдельная body-карточка (`AppInBitrixCard`) не годилась (b24ui `dark:`-темы vs vibecode-лендинг) — по желанию можно добавить нативную позже. |
| L7 | **Новая фоновая анимация hero** (не переиспользовать `HeroGraph` 4-й раз) | DONE | #46,#49,#52 | Прошёл варианты по выбору владельца: «Документ → строки CRM» (#46) → «Магнит Bitrix24» (#49) → финал **`HeroParticles.vue`** «частичное поле» (parallax-точки + редкие вспышки связей, #52). Все canvas, уважают `prefers-reduced-motion` (статичный кадр), пауза вне видимости/скрытой вкладки (раздельные `onScreen`/`tabVisible`), троттлинг ~30fps, `aria-hidden`. `HeroGraph.vue` и промежуточные `HeroDocFlow`/`HeroDocMagnet` удалены. |
| L6 | Подвал как у client-bank + ссылка на версию из гита | DONE | #41 + этот PR | Сначала ссылка «сборка &lt;sha&gt;» (#41). **Дорос до паритета с client-bank** (замечание владельца «подвал — у клиент банка верно»): `SiteFooter` — © ИП + УНП + город; email/Реквизиты/Политика/сборка; ряд «Бесплатные инструменты» (Конвертер валют, BBCode↔Markdown) + GitHub-иконка. |

---

## Журнал прогонов (обновляется каждым heartbeat)

- 2026-07-14 — **Владелец дал живой тестовый портал Б24 (webhook) + ключ DeepSeek.** Оба проверены
  вживую (git-ignored `.env`/`.env.b24test`). **Ядро продукта подтверждено end-to-end на реальном
  портале с реальным DeepSeek:** документ → DeepSeek-разбор → `runCrmSync` → создание сущности с
  товарными строками. Проверены **оба пути** — «счёт» → смарт-счёт (ownerType `SI`) и «накладная» →
  сделка (ownerType `D`, категория): поиск компании по ИНН (`RQ_INN`), валюта/НДС из портала, итог
  (`opportunity`) считается верно, `crm.item.productrow.set` пишет строки 1-в-1. Снят caveat
  «verify SI/D live». Инструмент повторной проверки — `pnpm live:crm [--ai] [--keep]`
  (`scripts/live-crm-sync.mjs` + ESM alias-loader). **Найдено и починено:** DeepSeek на net-документе
  (Итого без НДС → НДС → Всего) ошибочно ставил `priceIncludesVat=true` (итог занижался) — уточнил
  правило в `prompts/extract.ts` по структуре итогов; после фикса тот же документ даёт `false` и
  верный `opportunity` (882 вместо 735). **Прод-действие на владельце:** задать `ANTHROPIC_*` в env
  backend, чтобы AI-разбор PDF в демо заработал на проде.
- 2026-07-11 — карта заведена, цикл запущен, фундамент #26–#30 готов. Старт с P-приоритета.
- 2026-07-11 — P5-a (Excel в демо) + харднинг ридера смёржен (#37). P4 (визуальный харнесс
  `pnpm screenshot` + прогон по мобайлу) — смёржен (#38). Владелец добавил P6 (нативный b24ui вместо raw HTML) —
  переведены все in-portal-страницы, проверено скриншотами; на 5 проверяющих.
- 2026-07-11 — P6 (#39): сверка с офиц. b24ui-доками (убран мёртвый `variant`), нативный B24FileUpload,
  5 проверяющих (1 a11y-замечание устранено). Владелец уточнил: поле «Артикул» — поисковый пикер (P7,
  `catalog.productProperty.list` + порт async-подбора из client-bank), берётся следующим.
- 2026-07-11 — P7 (#40): поисковый пикер свойства каталога для «Артикул поставщика» (порт async-подбора
  из client-bank + backend `catalogPropertySearch`). 5 проверяющих поймали КРИТИКУ: двойной unwrap
  RestCall (пикер был бы всегда пуст в проде) — исправлено + тесты приведены к реальному контракту;
  плюс сид `:selected-option` (сохранённый код не пустой до открытия). Сверено вживую (B24_HOOK).
- 2026-07-11 — L6 (#41): ссылка «сборка &lt;sha&gt;» в подвале лендинга (трассируемость деплоя).
- 2026-07-11 — Владелец разблокировал: L2/L4 («это моё фото и визитка» → порт из client-bank),
  L5 (slug `shef.priceimport`), P1 (ключи DeepSeek внёс — E2E проверяет сам в портале), P5-b
  (уточнил: любой PDF → реальный результат/ошибка, 3/10мин → делаю на AI-конвейере).
- 2026-07-11 — L2+L4 (#43): визитка владельца + фото в hero (порт 1:1, адаптирован под продукт).
- 2026-07-11 — Лендинг-хром (этот PR): подвал (`SiteFooter`, паритет с client-bank) + меню
  (полный nav + мобильный drawer + ссылка «В Маркете» = L5). Замечание владельца «у клиент банка верно».
- 2026-07-11 — Владелец выбрал **новую фоновую анимацию hero «Документ → строки CRM»** (L7) вместо
  4-го переиспользования `HeroGraph`. В очередь после P5-b.
  Дальше: **P5-b** (демо на реальном AI-конвейере: любой PDF/скан → товары/контрагент/тип или ошибка,
  3/10мин, без хранения) → **L7** (новая анимация hero).
- 2026-07-12 — **Ревамп лендинга + демо-продукт + цикл аудита** (PR #45–#54). Демо: реальный
  AI-конвейер для PDF/скан/Word (#45), скачиваемые PDF-примеры (#48), реквизиты по странам
  (#50), валюта+прогресс+пробел (#51). Лендинг: hero и композиция как у client-bank на наших
  блоках и cyan (`HeroParticles`, `BriefForm`, `PartnerBadge`, `MobileBriefCta`, `useCardGlow`,
  og:image) — #52, #53. **Цикл аудита (#54)**: CSP на sha256-хэшах (убран `unsafe-inline`),
  nginx-потолок демо 2→6 МБ, глобальный лимит одновременных AI-разборов (503), НДС в AI-пути +
  синхронизация документации (08-demo, 01/02/04/10, эта карта). Каждый прогон — 5 проверяющих.
  Остаётся только **P1** (полный E2E in-portal) — BLOCKED (live-ключ DeepSeek + живой портал у владельца).
- 2026-07-12 — **P5-b смёржен (#45)**: любой PDF/скан/Word в демо → реальный AI-конвейер
  (`demoAi.ts`: маппер + DI-оркестратор под тестами; роутинг в `extract.post.ts`). 5 проверяющих
  поймали 2 замечания (ложное «Итого: 0» для прайса без количеств; 500 вместо честного {error} на
  битый multipart) — устранены. **L7 реализован (#46, на проверяющих)**: `HeroDocFlow.vue`.
  Дальше: дождаться CI #46 → мерж. Автономная очередь WORKLOG после этого — пуста (остаются P1 —
  live-ключ DeepSeek/портал; ассеты владельца выданы). Следующие прогоны — вероятно холостые.
- 2026-07-11 — L3 (этот PR): навигация лендинга (`LandingHeader` — липкая шапка, якоря, CTA). **Остальные
  лендинг-пункты уперлись в ассеты/факты владельца:** L2 (визитка) и L4 (фото) — нужен файл ФОТО владельца;
  L5 (маркет) — нужен реальный slug листинга (клиентбанковский `shef.bankimport` — другое приложение).
  Помечены BLOCKED. Автономно-доступные пункты WORKLOG исчерпаны: остаются P1 (E2E, live-ключ),
  P5-b (PDF-демо, решение владельца), L2/L4/L5 (ассеты владельца). Следующие прогоны — вероятно холостые.
