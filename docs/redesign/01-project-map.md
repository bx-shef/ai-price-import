# Карта проекта и процесс (редизайн procure-ai)

> Last reviewed: 2026-07-09

Канонический срез состояния редизайна: цель, этапы, что сделано / в работе / дальше,
блокеры, открытые вопросы и владельцы решений. Держим синхронно с
[`02-target-architecture.md`](02-target-architecture.md) и легендой честности ниже.

**Легенда честности** (как в эталоне `client-bank-alfa-by`):
`✅` — проверено вживую (на реальном портале/проде); `🧪` — код есть и покрыт тестами,
но вживую не гоняли; `📝` — только спроектировано/описано; `⛔` — блокер.

---

## 1. Цель

Переделать AI-импорт прайсов (`procure-ai`) с текущей 4-сервисной раскладки на
**единый Nuxt 4 + Nitro монолит** по образцу `client-bank-alfa-by` и превратить из внутреннего
инструмента под один портал в **облачное приложение для Маркета Bitrix24** (мультитенант, OAuth).
Сохраняем ценность (агент + промпт + MCP-инструменты + ядро извлечения текста) и добавляем то,
чего нет: дисциплину чистого ядра с тестами, очередь под нагрузку, публичный лендинг, маркетинг.
**Убираем весь код для ядра/коробки Б24** — вся интеграция через MCP поверх стандартного REST.

**Что импортируем:** любой **документ с табличной частью** (накладная/счёт/КП/прайс/спецификация —
не только «счёт»). Суть действия: **найти контрагента и внести товары** в целевую сущность; тип
цели — по правилам роутинга. **Языки документов: русский, белорусский, казахский** — подробно с
примерами/выводами в [`06-multilingual.md`](06-multilingual.md).

**Критерий готовности MVP редизайна:** менеджер на портале, где установлено приложение, загружает
документ с табличной частью → агент через изолированный MCP (стандартный REST по OAuth-токену)
создаёт целевую сущность (по правилам роутинга) с товарами; лендинг + листинг Маркета опубликованы;
деплой одним пайплайном (GHCR + Watchtower).

---

## 2. Целевая архитектура (одной фразой)

Nuxt-монолит: **лендинг + in-portal UI + Nitro-backend** в одном приложении; backend
оркеструет **агента Claude Code**, который ходит в Bitrix24 **только через изолированный MCP**;
MCP зовёт **стандартный REST Bitrix24** (ноль кода в коробке). Тяжёлые шаги (извлечение текста, прогон агента, запись в CRM) —
через **очередь BullMQ + Redis**. Детали — [`02-target-architecture.md`](02-target-architecture.md).

---

## 3. Этапы (roadmap редизайна)

| # | Этап | Статус |
|---|---|---|
| 0 | **Документация редизайна** — старая арх., карта, целевая арх., стек, маркетинг, политика данных, мультиязычность | ✅ готово |
| 1 | **Скелет Nuxt-монолита** — `app/` split + `server/api/health` + Vitest (unit) + ESLint + typecheck + SSG-сборка; чистое ядро (homoglyph/артикулы/роутинг/НДС/единицы/taxId) + 27 тестов; legacy-код перенесён в `legacy/` | 🧪 lint+typecheck+test+generate зелёные |
| 2 | **Встройка в Б24 (мультитенант)** — `useB24()` dual-mode, `/install` + `event.bind` + `placement` (веб **и мобильное приложение**), OAuth per-portal (Postgres, шифрование refresh), ленивый `ensureAccessToken` + **проактивный keep-alive крон** (продление `refresh_token` за ~3 дня до 180-дневного истечения), `app.option` | 🧪 ядро+связка: `b24Oauth`/`accessToken`/`secretCrypto`/`b24Rest`/`tokenStore`/`ensureAccessToken`/`portalRest`/`server/db` + эндпоинт `/api/b24/events` + тесты (нет: `/install` UI, worker-транспорта, крона) |
| 3 | **Настройки/маппинг портала** — форма настроек + чистое ядро маппинга: каталог, **поле артикула поставщика** (тип текст=построчно / строка=разделитель, задаёт админ), **целевая сущность + направление** (сделка/смарт-процесс/инвойс/КП; **роутинг**: ручной override → упорядоченные правила `{match: type|keyword → target}` → дефолт; `resolveTarget` — чистая функция), **тумблер сохранения файла** (дело — настраиваемое `crm.activity.configurable.add`, `layout` от приложения), **стратегия поиска/создания товара**, **единицы** (словарь→`catalog.measure` + дефолт + авто-создать), **чат ошибок + чат уведомлений**. НДС — ставки из Б24 (`crm.vat.list`). Налоговый ID — фикс. `RQ_INN`. Договор — не тут (Q8) | 🧪 ядро+UI: `mapping.ts`/`portalSettings` (`parsePortalSettings`/`defaultMapping`) + `appSettings`/`settingsHandler` + страница `/settings` (`settings.vue`/`useSettings`) + роуты `/api/settings` + тесты. Дальше: полировка формы, живое сохранение на портале |
| 4 | **Лендинг + маркетинг** — тёмная бренд-оболочка, `landing.ts`, HeroGraph, BriefForm, Метрика, OG, листинг Маркета | 🧪 `index.vue` (тёмная оболочка+hero) + `landing.ts` + `HeroGraph` + **публичное демо** (`DemoTryout`/`demoExtract`/`/api/demo/extract`, примеры рус/бел/каз, rate-limit, баннер доработки) + тесты. Дальше: BriefForm/Метрика/OG/листинг Маркета |
| 5 | **Ядро загрузки/извлечения** — `app/utils` парсинг + `server/` извлечение текста (pdftotext/OCR/office) в очереди; UI загрузки **только in-portal** (веб+мобильное приложение) с ручным выбором цели у файла; результат = статус + **ссылка «открыть»** сущность. Standalone-загрузка — кастом (маркетинг) | 🧪 ядро+связка: `textExtract` (диспетч+OCR-фолбэк), `extractRunners` (pdftotext/libreoffice/tesseract rus+bel+kaz+eng), `fileStore` (traversal-safe), `importUpload` (браузерный валидатор); **API** `POST /api/import/upload` + `GET /api/import/status` (фрейм-токен + верификация `member_id`). **UI in-portal `/import` — 📝** |
| 6 | **Агент + изолированный MCP** — порт промпта и инструментов (стандартный REST по токену портала) как первоклассный код с тестами; промпт распознаёт налоговый ID **с учётом страны и языка** (ИНН/УНП/БИН…, документ на любом языке → поиск по `RQ_INN`) | 🧪 `runAgent` (spawn+ретрай, **чистый экстрактор**), `extractJson`, `validateExtractedDocument` (нормализация untrusted JSON), `mcpConfig` (**исчерпывающий tool-lockdown + `agentSpawnEnv`** без секретов), `spawn` (санитизир. env + таймаут), `retry`, промпт `extract` (мультиязычный tax-id). **MCP-HTTP-сервер не нужен** (агент tool-less; тела инструментов зовёт `crm-sync` в процессе — реш. из ревью) |
| 7 | **Запись в CRM (стандартный REST)** — резолв цели (ручной→авто→дефолт) → `crm.item.add` + `crm.item.productrow.set` (НДС: ставки из `crm.vat.list`, `taxIncluded` единый на документ, ставки по позициям могут различаться); файл на **общий Диск** (папка приложения → подпапка по месяцам) + **настраиваемое дело** `crm.activity.configurable.add` с файлом (`layout` от приложения; тумблер сохранения файла). Без коммента/UF-поля. Lookups по маппингу (несколько совпадений → мин. ID + предупреждение); **без дедупа** (всегда создаём); **матрица нестыковок** (поставщик/товар/валюта/единица) → предупреждение или **чат ошибок**; предпросмотра нет | 🧪 ядро+**связка**: `crmWrite`/`companyLookup`/`disk`/`configurableActivity`/`crmSyncCore.runCrmSync`; читатели портала `portalVat`/`portalCurrency`/`productLookup` (✅ формы вживую), `resultStore` (идемпотентность); `liveDeps`+`worker`+плагин запуска. НДС ✅ вживую. **✅ Сквозной путь пройден вживую 2026-07-09** (засеянный портал + DeepSeek: компания по RQ_INN, сделка с opportunity, персист позиций, файл на Диск, дело+файл через `todo.add`, чат) + **✅ article-поиск товара** (свойство на iblock 25; `%PROPERTY_<id>`+точная сверка, оба варианта). Дальше: `configurable.add` в OAuth-контексте, идемпотентность дубля |
| 8 | **Метрики, обратная связь, оператор** — метрики 2 уровня (мотивирующие всегда на `/app` + детальные `/metrics`); **отзывы сотрудников** (👍/👎/💡+текст) → **закрытый репо издателя** (outbox) + **согласие на файл** (политика данных); **сигналы агента**; **страница оператора как в эталоне** (авторизация + `/queues` ECharts); health, AI-телеметрия, политика диагностики | 🧪 `metricsStore` (счётчики); UI 2 уровней, отзывы, оператор — 📝 |
| 9 | **Деплой + CI/CD** — Docker multi-target (+ pdftotext/libreoffice/tesseract в образе backend), CSP-hashing, GHCR + Watchtower, required-check `ci` | 📝 |

Лендинг (этап 4) не зависит от ядра — можно вести параллельно. **Этапа деплоя PHP-контроллеров
больше нет** (нет кода в коробке).

**Метрики/обратная связь/оператор** (этап 8): метрики 2 уровня (мотивирующие на `/app` + детальные
`/metrics`); отзывы сотрудников (👍/👎/💡) + сигналы агента (уверенность/проблемы → ручной разбор,
алерты дрейфа); служебная зона оператора как в эталоне (login + `/queues`). Детали —
[`02-target-architecture.md`](02-target-architecture.md) §7. Также аргумент ценности в маркетинге.

---

## 4. Сделано / в работе / дальше

**Сделано (🧪 код+тесты, если не помечено ✅ вживую):**
- Полный набор `docs/redesign/` (00–06) + корневой `CLAUDE.md`.
- **Этап 1 (скелет)** — Nuxt 4 + Nitro: `package.json`/`nuxt.config`/`tsconfig`/`eslint`/`vitest`
  (2 проекта), `app/` split, `server/api/health.get.ts`, SessionStart-хук; чистое ядро
  (`homoglyph`, `supplierArticles`, `routing/resolveTarget`, `vat`, `units`, `taxId`, `build`).
  Legacy → `legacy/`. `lint`+`typecheck`(Nuxt4 split-tsconfig)+`generate` зелёные.
- **Ядро OAuth/токенов (этап 2):** `b24Oauth`, `accessToken` (ленивый/keep-alive предикаты),
  `secretCrypto` (AES-GCM), `b24Rest` (типизированная `B24RestError` + **SSRF-гард** `isSafeB24Domain`),
  `tokenStore` (Postgres, write-once `application_token`), `ensureAccessToken` (refresh + ретрай),
  `portalRest` (`makePortalRestCall(memberId)` → **живой `RestCall`**), `server/db/` (схема + ленивый
  pg-клиент + плагин миграции). **HTTP-эндпоинт `/api/b24/events`** (install/unregister, синхронная
  запись, вердикт по сохранённому per-portal токену).
- **Ядро записи в CRM (этап 7):** `companyLookup` (RQ_INN), `crmWrite` (productrow/create-target),
  `disk` (общий диск + папка по месяцам), `configurableActivity`, `crmSyncCore.runCrmSync`
  (роутинг→поставщик→НДС/единицы→позиции→создание + матрица нестыковок + идемпотентность).
- **События/очередь:** `b24Events` (парсинг), `b24EventsHandler` (вердикт), `queue/topology` (контракты).
- **Настройки:** `portalSettings`/`appSettings` (парсинг+чтение/запись `app.option`), `frameAuth`,
  роуты `/api/settings`.
- **Пайплайн загрузки→извлечение→агент→CRM (этапы 5–7) — построен и связан:**
  - Извлечение: `textExtract` (диспетч по расширению + OCR-фолбэк сканов), `extractRunners`
    (pdftotext/libreoffice/tesseract rus+bel+kaz+eng), `fileStore` (traversal-safe).
  - Агент-экстрактор: `runAgent` (spawn+ретрай; ретрай и на exit-0 API-error конверте),
    `validateExtractedDocument` (untrusted JSON → безопасный документ; числа/валюта/placeholder-строки),
    `mcpConfig` (**исчерпывающий deny-list + `agentSpawnEnv`**), `spawn` (санитиз. env+таймаут),
    промпт `extract`. Резолюция «агент только извлекает; поиск/запись — детерминированно в `crm-sync`».
  - Проводка: `liveDeps` (связка чистых обработчиков со стором/REST/агентом/очередями), `worker`
    (BullMQ-воркеры + `buildLiveInfra`), плагин `server/plugins/queue` (старт воркеров), читатели
    `portalVat`/`portalCurrency`/`productLookup`, `resultStore` (идемпотентность), стор дедупа дел.
  - Стораджи клиентских данных с минимизацией: `textStore`/`docStore`/`fileStore` — данные живут в
    одном месте и удаляются на терминальных путях; `deletePortal` чистит всё при удалении приложения.
  - **API in-portal:** `POST /api/import/upload` + `GET /api/import/status` — фрейм-токен +
    **верификация `member_id`** (`resolveFrameMember`: проверка токена → member по домену; клиентскому id не доверяем).
- **НДС 1-в-1 ✅ подтверждён вживую** (`crm.item.productrow.set` без патча ядра). Формы
  `crm.vat.list`/`crm.currency.list`/`crm.product.list` — ✅ вживую.
- **Живая проверка** REST на `B24_HOOK`. **264 unit-теста**, зелёные; `typecheck` покрывает **и `app`, и `server`**.
- **3 больших ревью 5 специалистов** (скелет/ядро; агент/пайплайн; связка/API) — замечания устранены,
  включая **CRITICAL**: неполный tool-lockdown агента (Read/Glob/Task оставались доступны → эксфильтрация
  секретов из `.env`); **HIGH** по 1-в-1 (тихая обрезка позиций, `priceIncludesVat`-дефолт, парсинг чисел).

**В работе / дальше:**
- **UI (построены, связаны с API):** ✅ `/app` (дашборд), `/import` (drag-drop + список задач),
  `/settings` (форма маппинга, `useSettings`), `/login`+`/queues` (оператор, `useAuth`+ECharts),
  публичный лендинг `/` (тёмная оболочка + hero + **демо**). **Осталось:** полировка на b24ui,
  in-portal `/install` UI.
- Этап 2 UI: `useB24()`, `/install` (`event.bind`), `placement`; keep-alive крон продления токенов.
- В проводке `crm-sync`: файл на Диск + настраиваемое дело; чат ошибок/уведомлений (article-поиск товара — ✅ вживую).
- Метрики/оператор UI обратной связи (8), деплой + образ backend с бинарями извлечения (9).

---

## 5. Что переносим из старого проекта

| Актив | Куда в новой раскладке | Примечание |
|---|---|---|
| Промпт `prompts/main.md` | `prompts/` или `server/agent/` | Ценность как есть; ревизия правил (НДС→штатный, единицы) |
| Логика инструментов (поставщик/товар/сделка) | новый MCP-пакет `mcp/tools/*` | **Переписать на стандартный REST** (`crm.*`/`catalog.*`) вместо кастомных `shef:purchase.api.*`. **Поиск договора не переносим** (Q8) |
| Ядро извлечения текста | `server/` + утилиты | pdftotext/tesseract/office; в очереди |
| Homoglyph-логика | `app/utils/*` (чистая, тесты) | Уже почти чистая функция |
| OAuth/мультитенант из старого MCP | `server/utils/*` + MCP | **Оживает**: per-portal токены, `ensureAccessToken`, `makePortalRestCall` (как эталон) |
| Метрики + **обратная связь** | `server/` + Postgres | Пересобрать: метрики 2 уровня; отзывы сотрудников (👍/👎/💡) + сигналы агента |
| Авторизация оператора | `server/utils/session.ts` + `/login` | Модель эталона (HMAC-cookie, rate-limit) для служебной зоны |

**Не переносим:** весь `b24-controller/` (PHP-модуль коробки, `procure*.php`, **патч ядра НДС** —
решение Q7); мёртвый чат-бот (#241); вендоренный `mcp/` как git-subtree; хардкод инфоблоков/полей
(заменяем пер-портальным маппингом); i18n на 19 локалей (только `ru`, перевод позже);
**eval-харнесс** (убран — вместо скоринга требование 1-в-1, см. ниже).

### Детальные решения (обсуждение документов)

| # | Решение |
|---|---|
| D1 | **Монорепо** — MCP не выносим в отдельный репозиторий, живёт в этом же (`mcp/`). |
| D2 | **Worker внутри backend-процесса** в MVP; выделенный worker-контейнер — под нагрузкой. |
| D3 | **Postgres** — OAuth-токены, идемпотентность джоб, метрики; Redis — только очереди. (Бизнес-дедупа импорта нет — см. решение по дубликатам.) |
| D4 | **Локализация только `ru`**; i18n-инфраструктуру и переводы добавим позже. |
| D5 | **Eval-скоринг-харнесс убран.** Требование точности — позиции и цены **1-в-1 со счётом**. Проверка — юнит-тесты чистого ядра + **golden-фикстуры** «текст документа → ожидаемые productrow» (дешёвый регресс-guard без скоринга; ловит дрейф извлечения при смене промпта/модели — рекомендация ревью). |

---

## 6. Открытые вопросы и решения владельца

| # | Вопрос | Статус | Владелец |
|---|---|---|---|
| Q1 | Целевая архитектура: монолит Nuxt + изолированный MCP | ✅ подтверждено | владелец |
| Q2 | Стратегия: greenfield в этом репо | ✅ подтверждено | владелец |
| Q3 | Организация docs: `docs/redesign/` | ✅ принято | Claude |
| Q4 | Продукт: облачное приложение для **Маркета Bitrix24** (мультитенант) | ✅ принято — Маркет | владелец |
| Q5 | Провайдер LLM: **DeepSeek** (Anthropic-совместимый endpoint) | ✅ принято — DeepSeek | владелец |
| Q6 | Публичный лендинг как у эталона | ✅ да, делаем | владелец |
| Q7 | НДС-патч ядра / код в ядре Б24 | ✅ принято — **убираем всё, только MCP + стандартный REST** | владелец (CTO) |

Все ключевые развилки закрыты. Вопросы, всплывшие из решений Q4/Q7:

| # | Вопрос | Статус |
|---|---|---|
| Q8 | Поиск договора | ✅ **не ищем** в стандартном приложении; подбор договора — платная доработка на сервере клиента (маркетинг-фича) |
| Q9 | Требуемые scopes приложения | ✅ **`crm`, `catalog`, `disk`, `im`, `placement`** (`user_brief` не нужен — был для визитки эталона) |
| Q10 | Издатель/бренд для листинга Маркета | ✅ **ИП Шевчик И.С.** |
| Q11 | Набор настроек единиц измерения | ✅ **словарь (документ→`catalog.measure`) + дефолт + при отсутствии авто-создать + ошибка в чат** |
| Q12 | Правила НДС | ✅ **ставки из настроек Б24 (`crm.vat.list`); нет такой ставки → ошибка; `taxIncluded` единый на документ (разнобой → ошибка); ставки по позициям могут различаться** |
| Q13 | Очередь ручного разбора | ✅ **не нужна** — исходов достаточно (предупреждения + чат ошибок) |

## 7. Риски и рекомендации (из ревью 5 специалистов)

- **✅ СНЯТ вживую — НДС без патча ядра работает.** Проверено на `B24_HOOK`: `crm.item.productrow.set`
  с `price=100, quantity=2, taxRate=22, taxIncluded=Y` (BYN) → `priceBrutto=100`, `priceNetto=81.97`,
  **сделка: Итого=200.00, НДС=36.07** (200×22/122 ✓). Патч ядра НЕ нужен — он был нужен для **legacy**
  `crm.deal.productrows`, а универсальный `crm.item.productrow.*` считает НДС сам. Тезис Q7 «ноль кода
  в ядре» подтверждён. Бонус: **free-form позиция** (без `productId`, только `productName`) — работает.
- **Рекомендация: вертикальный спайк раньше инфраструктуры.** Сквозной срез
  upload→extract→агент→MCP→`crm.item.add`+`productrow.set` на живом портале (один хардкод-портал,
  одна тест-накладная, без очереди/мультитенанта/лендинга) — проверить связку и фидельность до
  постройки этапов 2–5 вокруг неё. Самое непроверенное — AI-слой (этапы 6–7), а он сейчас в конце.
- **DeepSeek/КНР — не только заметка, а риск листинга.** Отправка счетов/реквизитов в КНР-инференс
  несовместима с 152-ФЗ/коммерческой тайной части покупателей Маркета. Провайдер-агностичность
  (`ANTHROPIC_*`) — правильная страховка; **до публикации листинга (этап 4) согласовать с владельцем**,
  дефолтный провайдер для публичной версии (DeepSeek vs не-КНР). **Листинг не публиковать, пока один
  реальный портал не проведёт реальный импорт.**
- **worker/mcp — отдельные контейнеры раньше «под нагрузкой».** agent-run спавнит тяжёлый подпроцесс
  (OCR/Claude Code) — не держать в одном процессе с HTTP-backend дольше необходимого (Dockerfile split уже заложен).
- **Блокеры (`⛔`):** доступ к живому порталу для валидации НДС/OAuth (частично снят — есть `B24_HOOK`);
  юр-согласие клиента на облачный LLM.
